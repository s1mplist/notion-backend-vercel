[mypy]
# Use the src directory as import root so absolute imports like `services.*` resolve.
python_version = 3.11
mypy_path = src
namespace_packages = True
# Don't fail the entire run on missing third-party stubs - these can be addressed separately.
ignore_missing_imports = True
